# 第三阶段开发 TODO（全员薪资与 OA 扩展）

更新时间：2026-07-09
状态：**未启动**——按 PRD 目标 2026-09 起分批，2027-01 底全量收口
PRD：`docs/全员薪资与OA扩展-第三阶段PRD.md`（3A~3E 五批次，每批独立上线、独立验收、独立回滚）
后续：第三阶段收口后的体验打磨见 `PHASE4_TODO.md`

> 总原则：分批不大爆炸；新接口走独立命名空间（`/api/att2/*`、`/api/oa/*`、`/api/pay2/*`），不动一阶段教师链路；每批 feature flag 灰度（先选 1 个部门试点 2 周）；规则/方案全部版本化+生效日期，禁止改当月已结算规则。

## 入场条件（开工前确认）

- [ ] 第二阶段生产上线且稳定运行（迁移窗口 + 学校 UAT 完成）。
- [ ] 决策项 R1 拿到答复：打卡校验方式（校园 Wi-Fi/IP 段 vs 定位围栏）+ 合规告知文本（3A 前）。
- [ ] 学校提供：全员班次表/排班规则、各岗位工资制度书面确认稿（R2，可按岗位分批给，对应岗位平行核算前到位）。
- [ ] 决策项 R6：假期额度基线（年假/寒暑假规则，3C 前）。

## 3A 全员移动考勤（2026-09）

数据层与域逻辑：
- [ ] 新集合落地：shifts / shiftAssignments / clockRecords（只增不改）/ workEvents / attendanceDaily，postgres 003 后续补丁 SQL + 运行时集合注册（沿 storage.js 集合模式）。
- [ ] `server/attendance2.js` 域模块：班次模型（固定/轮班/弹性）、打卡写入（幂等，沿一阶段签到幂等模式）、异常自动标记（迟到/早退/缺卡/旷工，仅标记不扣款）、工时事件录入+员工确认双步。
- [ ] 打卡校验器（按 R1 决策实现 Wi-Fi/IP 段 或 定位围栏二选一）；定位方案需首次使用知情同意记录 + 坐标保留 ≤12 个月的清理任务。

接口与前端：
- [ ] `/api/att2/*` 路由：打卡、我的考勤、班次维护（学部负责人）、工时事件、日汇总查询（全部分页 + scope 过滤，复用二阶段权限中间件）。
- [ ] 移动端打卡页（全员）：大按钮打卡 + 当日状态 + 本月异常列表；学部负责人班次/排班维护视图；工时事件录入与确认视图。
- [ ] 教师课时签到与全员打卡明确分流（教师课时仍走教室动态码，界面互不混淆）。

质量：
- [ ] 打卡峰值压测：1500 人 7:40-8:10 集中打卡，目标 100 req/s 写入（SQL 层重新压测，复用 scripts/load-test.js 框架）。
- [ ] `tests/att2.test.js`：幂等重试不重复流水、异常标记规则、原始记录不可改断言、班次边界（跨天轮班）。
- [ ] 试点部门灰度 2 周：打卡流水完整率 ≥ 99%，异常标记与人工核对一致（验收 7.3A）。

交付效果：非教学岗位有客观出勤记录，异常自动标记，原始流水可追溯。

## 3B 各岗位工资方案引擎（2026-09 → 10）

- [ ] PAY2-1 原子规则引擎：固定额 / 档位表 / 数量×单价 / 阶梯分段 / 比例 五种规则类型 + 组合器；方案（paySchemes/schemeRules/schemeVersions）版本化 + 生效日期。
- [ ] 岗位方案配置化落地（零岗位写死代码）：行政干部（含兼课课时复用一阶段计算）、司机（工时事件联动）、校医、生活老师（多维工时×单价）、食堂、幼儿园、招生（业绩录入+总校确认，R4 口径确认后）、安保。
- [ ] PAY2-2 结算流程复用：员工确认 → 主管审批 → 财务复核 → 锁定 → 导出，工资单明细行含计算依据（规则版本引用）。
- [ ] 主管审批语义：按组织架构 reportsTo/部门负责人解析（不新建角色）。
- [ ] 全员月度结算后台任务化：全员工资生成 < 5 分钟（复用排课异步任务框架）。
- [ ] `tests/pay2.test.js`：五种原子规则边界、方案版本切换不影响已结算月、工资金额可见性（主管不可见金额）。
- [ ] **每岗位平行核算 1 个月**：系统结果 vs 财务手工表，差异逐条归因清零后该岗位才切换（验收红线，逐岗位签字）。

交付效果：全员工资进系统结算，方案可配置可版本化。

## 3C OA 审批（2026-10 → 11）

- [ ] 通用审批引擎抽取：在 hr_flows 数据结构上扩展 oaFlows/oaFlowSteps（同构），支持会签/或签、按组织架构解析审批人；流程模板由开发配置（明确不做自助流程设计器）。
- [ ] OA-1 请假：七类假种 + 额度台账（leaveBalances）+ 证明材料附件；批准结果写入考勤抵扣异常。
- [ ] OA-2 加班：前置申请 → 打卡工时复核 → 主管确认 → 生成加班工时事件（供 3B/3D）。
- [ ] OA-3 补卡：月度次数上限（默认 3 次，学校确认）+ 超限特批。
- [ ] OA-4 调课审批升级：教师发起 → 教务审批 → 自动执行调课（复用一阶段调课接口与全局冲突校验；保留行政直批开关作为回退）。
- [ ] OA-5 待办中心统一：与二阶段人事审批同入口同角标（SSE 推送主题扩展），移动端可审批。
- [ ] `tests/oa.test.js`：额度扣减与回冲、批假抵扣考勤异常、会签/或签步进、调课审批通过后课表生效与冲突拦截。
- [ ] 审批平均时长看板（验收 7.3C 要求可见）。

交付效果：考勤异常有制度化出口，调课进入审批闭环。

## 3D 考勤扣款与薪资联动（2026-11 → 12）

- [ ] 决策项 R3 到位后录入扣款规则表（deductionRules，版本化）：迟到/早退分钟阶梯、缺卡、旷工按日扣、空堂制度性扣款（可选项）——**系统只做规则执行者，金额公式以学校书面稿为准**。
- [ ] DED-2 预扣清单（deductionItems）：月结算前生成 → 员工可见 → 申诉（走 OA）→ 3 天申诉期后入工资。
- [ ] DED-3 请假互斥：已批假期覆盖的异常不扣款（与 3C 联动断言）。
- [ ] DED-4 工资单扣款逐条列示来源（哪天/什么异常/什么规则版本/多少钱）。
- [ ] `tests/ded.test.js`：阶梯边界、假期覆盖不重复扣、申诉期内不入账、规则版本引用正确。
- [ ] 全链路演练：预扣清单 → 申诉 → 入工资（验收 7.3D）。

交付效果：迟到早退旷工自动、透明、可申诉地进入工资。

## 3E 津贴评审、报销与全员报表（2026-12 → 2027-01）

- [ ] AWD-1 名师津贴：申报 → 评审组会签（总校按学期指定临时会签组）→ 公示期 → 按学期发放计划入工资；考核不通过余额取消。
- [ ] AWD-2 课题奖励：申报 → 审核 → 公示 → 一次性/分期发放留痕。
- [ ] AWD-3 寒暑假工资资格：学年度出勤+续聘状态自动初筛 + 总校终审 → 驱动假期月工资生成。
- [ ] EXP-1 报销：申请（附件）→ 主管 → 财务 → 打款标记（仅流程与台账，不对接银行）。
- [ ] RPT-1/2 报表：全员工资总表（月度/分部门/同比环比）、人力成本看板（仅总校+财务可见）。
- [ ] RPT-3 导出：全员工资表、分部门表、银行报盘 CSV（格式按 R5 与代发银行确认）。
- [ ] `tests/awd-rpt.test.js`：发放计划分期入账、资格初筛规则、报表汇总与明细对账一致。
- [ ] 名师津贴完整学期周期演练 + 报表/报盘格式财务书面确认（验收 7.3E）。

交付效果：津贴/奖励/假期工资闭环，总校看全貌。

## 验收口径（红线）

- [ ] 每批次上线前：全部既有测试套件回归通过 + 该批次新增套件通过。
- [ ] 3B 任何岗位未通过平行核算不得切换该岗位真实发薪。
- [ ] 扣款/津贴规则必须能导出完整证据链（申请、审批、打卡原始记录、规则版本）。
- [ ] 他人工资金额仅财务与总校可见；主管审批看不到金额（自动化越权测试）。
- [ ] 范围蔓延控制（R7）：新岗位/新制度需求一律进下一批次评审，不插队当前批次。
