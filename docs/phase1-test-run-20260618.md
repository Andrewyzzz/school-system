# 第一阶段测试执行记录 2026-06-18

测试日期：2026-06-18  
测试人：Codex  
代码版本：`ca57bc3 Update production test run results`
测试范围：部署检查、本地自动化回归、本地页面冒烟、接口冒烟、生产接口冒烟、第一阶段 P0 主流程抽测。
结论：最新代码已部署到生产服务器，服务器本机健康检查通过；本地自动化回归通过；本地页面和接口主要角色链路可用；生产域名 DNS 当前解析到 `198.18.0.20/198.18.0.19`，未解析到服务器 `47.76.189.5`，导致正常公网访问失败；强制解析到 `47.76.189.5` 后生产 HTTPS 健康检查通过；生产 readiness 失败，原因是没有已发布课表和已锁定工资数据。由于公网账号安全触发锁定风险，追加执行了一轮本地非公网测试。本地补测后，财务端已能渲染已锁定工资，老师薪资接口已收窄为总薪资摘要；课表按当前版本作为最新版验收；生产域名、二维码和真机扫码先暂缓。

## 1. 部署执行结果

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 本地代码状态 | 通过 | 工作区开始时干净，最新提交为 `f09ed2a410d65975b96b77e25accee4c81799274` |
| 生产站点 DNS 检查 | 不通过 | `fuyuanschool.org` 解析到 `198.18.0.20`，`www.fuyuanschool.org` 解析到 `198.18.0.19`，不是服务器 `47.76.189.5` |
| SSH 生产部署 | 通过 | 用户明确允许后，成功登录服务器执行部署 |
| 生产代码拉取 | 通过 | `/opt/school-system` 从 `650b0d0` fast-forward 到 `f9632e2` |
| 生产服务重启 | 通过 | `systemctl restart school-system` 后 `systemctl is-active school-system` 返回 `active` |
| 生产本机健康检查 | 通过 | 服务器内 `curl http://127.0.0.1:4173/api/health` 返回 `status=ok` |
| 生产公网强制解析健康检查 | 通过 | `curl --resolve fuyuanschool.org:443:47.76.189.5 https://fuyuanschool.org/api/health` 返回 `status=ok` |
| 生产正常域名访问 | 不通过 | 正常 DNS 解析错误，浏览器打开 `https://fuyuanschool.org` 返回 `ERR_CONNECTION_CLOSED` |

### 1.1 已执行生产部署命令

已在服务器执行：

```bash
ssh -i /Users/yyzzz/Downloads/school_system.pem root@47.76.189.5
cd /opt/school-system
git pull --ff-only origin main
node --check app.js
node --check server/payroll.js
node --check server/storage.js
node --check server/server.js
node --check server/importTeachers.js
systemctl restart school-system
systemctl is-active school-system
curl -sS http://127.0.0.1:4173/api/health
curl --resolve fuyuanschool.org:443:47.76.189.5 -sS https://fuyuanschool.org/api/health
```

部署结果：

```text
before: 650b0d0
after: f9632e2
school-system: active
local health: ok
forced HTTPS health: ok
```

## 2. 自动化回归结果

| 检查项 | 结果 | 输出 |
| --- | --- | --- |
| `node --check app.js` | 通过 | 无错误输出 |
| `node --check server/payroll.js` | 通过 | 无错误输出 |
| `node --check server/storage.js` | 通过 | 无错误输出 |
| `node --check server/server.js` | 通过 | 无错误输出 |
| `node --check server/importTeachers.js` | 通过 | 无错误输出 |
| `node tests/phase1-production.test.js` | 通过 | `phase1 production checks passed` |
| `git diff --check` | 通过 | 无错误输出 |

自动化脚本覆盖并通过：

- 1000 个教师账号初始化。
- 行政排课缺少任课老师配置时拦截。
- 补齐任课老师后生成无冲突课表。
- 体育每天最多 1 节等课程约束。
- 发布课表。
- 会话创建与撤销。
- 通知定向和已读。
- 实际完成课次进入工资。
- 老师确认、教务审批、总校审批。
- 财务生成、复核、锁定。
- 已锁定工资禁止直接重算。
- 工资解锁、修改工资档案、重新生成、再次锁定。
- 工资导出包含规则版本和补充项明细。
- 第一阶段 readiness 检查在自动化内存数据中通过。

## 3. 本地测试服务

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 本地服务端口 | 通过 | 使用 `PORT=4185 node server/server.js` 启动 |
| 浏览器打开 | 通过 | `http://127.0.0.1:4185/?v=test-run-20260618` 正常显示 |
| 控制台错误 | 通过 | 页面打开、财务页、行政页、老师页均未捕获前端 error |
| 本地健康检查 | 通过 | `status=ok`，`teacherCount=1000`，`accountCount=1004` |

健康检查返回：

```json
{
  "status": "ok",
  "service": "school-system-phase1-api",
  "teacherCount": 1000,
  "accountCount": 1004,
  "seedVersion": "phase1-20260611"
}
```

## 4. 接口冒烟测试

| 用例 | 结果 | 实际结果 |
| --- | --- | --- |
| 财务登录接口 | 通过 | `finance / 123456` 登录成功，角色为 `finance` |
| 行政登录接口 | 通过 | `admin / 123456` 登录成功，角色为 `admin` |
| 老师登录接口 | 通过 | `teacher0001 / 123456` 登录成功，角色为 `teacher` |
| 人员分页接口 | 通过 | `/api/personnel?page=1&pageSize=20` 返回 200，总人数 `1003` |
| 教师分页接口 | 通过 | `/api/teachers?page=1&pageSize=20` 返回 200，总教师数 `1000` |
| 薪资规则接口 | 通过 | `/api/payroll-rules` 返回 200，`teacherSalaryScheme.settlementMode=actualCompletedLessons` |
| 教室列表接口 | 通过 | `/api/classrooms` 返回 200，教室数 `110` |
| 工资导出接口 | 通过 | `/api/payroll/export?month=2026-06` 返回 200，当前导出总数 `1` |
| 老师本人接口 | 通过 | `/api/teachers/me` 返回 200，老师为 `李明` |
| 第一阶段 readiness | 不通过 | `passed=false`，失败项见问题 `BUG-20260618-002` |

## 4.1 生产接口冒烟测试

生产测试因 DNS 解析错误无法直接使用 `https://fuyuanschool.org`，本轮使用 `curl --resolve fuyuanschool.org:443:47.76.189.5` 强制解析到服务器公网 IP 执行。

| 用例 | 结果 | 实际结果 |
| --- | --- | --- |
| 生产健康检查 | 通过 | `status=ok`，`teacherCount=1000`，`accountCount=1004`，`seedVersion=phase1-20260611` |
| 生产财务登录 | 通过 | `finance / 123456` 登录成功，返回 token |
| 生产教师分页 | 通过 | 总教师数 `1000`，第 1 条为 `李明` |
| 生产薪资规则 | 通过 | `teacherSalaryScheme.settlementMode=actualCompletedLessons`，版本 `fuyuan-dedicated-teacher-2026-v1` |
| 生产 readiness | 不通过 | `schedule_conflict=false`、`teacher_schedule=false`、`payroll_lock=false` |
| 生产首页 HTML | 通过 | 强制解析后首页 HTML 正常返回 |
| 生产教室二维码页 HTML | 通过 | 强制解析后 `classroom.html` 正常返回二维码相关页面内容 |

readiness 失败项：

```json
[
  {
    "key": "schedule_conflict",
    "label": "行政可生成无冲突课表",
    "passed": false,
    "detail": "已有无冲突草稿或可通过排课接口生成"
  },
  {
    "key": "teacher_schedule",
    "label": "发布后老师端能看到本人课表",
    "passed": false,
    "detail": "当前发布课次 0 节"
  }
]
```

## 5. 页面级 P0 测试

| 测试用例 | 结果 | 记录 |
| --- | --- | --- |
| TC-P0-001 后端健康检查 | 通过 | 本地 4185 健康检查通过；生产健康检查待 SSH 部署后执行 |
| TC-P0-002 财务账号登录 | 通过 | 财务账号可登录；能看到财务首页、薪资结算；看不到行政排课和老师签入入口 |
| TC-P0-003 行政账号登录 | 通过 | 行政账号可登录；能看到行政排课、人员列表、教师导入 |
| TC-P0-004 老师账号登录与数据隔离 | 通过 | 老师可见工作台、我的课表、签入/签出入口、薪资汇总；看不到薪资结算和行政排课；未发现逐课金额 |
| TC-P0-005 1000 个教师账号规模检查 | 通过 | 接口返回教师总数 1000；人员总数 1003；分页接口正常 |
| TC-P0-006 行政配置年级班级数 | 部分通过 | 页面存在普通班、实验班配置和保存入口；本轮未修改真实配置 |
| TC-P0-007 行政配置年级课程 | 部分通过 | 页面存在课程名称、每周节数、每节时长分钟、新增课程、保存课程规则入口 |
| TC-P0-008 行政配置课程硬约束 | 部分通过 | 页面存在课程、禁排星期、禁排节次、原因、添加硬约束入口 |
| TC-P0-009 按班配置任课老师 | 部分通过 | 页面存在大量按班老师选择框和“保存班级老师”入口 |
| TC-P0-010 缺少任课老师时禁止排课 | 通过 | 点击保存班级老师后，一键生成排课仍禁用，并提示缺少任课老师配置 |
| TC-P0-011 一键生成无冲突课表 | 自动化通过 / 页面未手工执行 | 当前页面需要逐班补齐老师配置，手工未继续生成；自动化脚本已覆盖通过 |
| TC-P0-012 课程硬约束生效 | 自动化通过 / 页面未手工执行 | 自动化脚本覆盖体育每天最多 1 节等约束 |
| TC-P0-013 发布课表到老师端 | 自动化通过 / 当前运行库不通过 | 自动化脚本通过；当前本地运行库发布课次为 0，见问题 `BUG-20260618-002` |
| TC-P0-014 老师端手机课表显示 | 当前版通过 | 用户确认课表就按照当前版本，作为最新版验收 |
| TC-P0-015 教室电脑显示动态二维码 | 暂缓 | 二维码相关先不处理 |
| TC-P0-016 老师扫码签入 | 暂缓 | 二维码和生产域名相关先不处理 |
| TC-P0-017 老师扫码签出 | 暂缓 | 二维码和生产域名相关先不处理 |
| TC-P0-018 实际完成课次计入工资 | 自动化通过 | 自动化脚本确认课时工资依据包含“实际完成课次” |
| TC-P0-019 月度工作量确认流程 | 自动化通过 | 自动化脚本覆盖老师确认、教务审批、总校审批 |
| TC-P0-020 财务生成、复核、锁定工资 | 自动化通过 | 自动化脚本覆盖生成、复核、锁定 |
| TC-P0-021 财务解锁重算工资 | 自动化通过 | 自动化脚本覆盖解锁、改档案、重算、再锁定 |
| TC-P0-022 工资导出 CSV | 接口通过 / UI 按钮可用 | 导出接口按学部、年级、搜索条件返回 1 条；Codex 内置浏览器不支持真实下载事件，需普通浏览器复测落盘 |

## 5.1 本地非公网完整链路补测

补测时间：2026-06-18
补测方式：不访问公网域名，使用 `http://127.0.0.1:4184/` 本地服务验证。

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 本地服务启动 | 通过 | 使用 `PORT=4184 node server/server.js` 启动，健康检查返回 `status=ok` |
| 本地健康检查 | 通过 | `teacherCount=1000`，`accountCount=1004`，`seedVersion=phase1-20260611` |
| 语法检查 | 通过 | `app.js`、`server/payroll.js`、`server/storage.js`、`server/server.js`、`server/importTeachers.js` 均通过 `node --check` |
| 自动化回归 | 通过 | `node tests/phase1-production.test.js` 输出 `phase1 production checks passed` |
| 生成并发布本地排课数据 | 通过 | 小学部一年级生成 `200` 节课，冲突 `0`，发布 `200` 节 |
| 排课求解器表现 | 需观察 | OR-Tools CP-SAT 本轮未在限制时间内找到可行解，系统 fallback 到 `advanced-constraint-search`，最终成功生成无冲突课表 |
| 本地 readiness | 通过 | 完成发布课表和 `T0001` 锁定工资后，readiness 全部通过 |
| 教师端登录 | 通过 | `teacher0001 / 123456` 可登录，只看到老师端栏目 |
| 教师端课表 | 通过 | `teacher0001` 能看到已发布课表，`6月15日 - 6月21日` 有 `5` 节语文课；用户确认按当前版本作为最新版 |
| 教师端总薪资展示 | 页面通过 | 可见总薪资为 `¥8,554`，页面未展示每节课金额 |
| 教师端薪资接口 | 通过 | 老师角色只返回本人身份、月份、总实发、生成状态、确认状态，不返回工资项目快照和逐课工资明细 |
| 教师访问其他老师工资 | 通过 | `teacher0001` 请求 `T0002` 薪资返回 `403`，提示只能访问本人教师数据 |
| 财务端登录 | 通过 | `finance / 123456` 可登录并进入薪资结算 |
| 财务端筛选 | 通过 | 薪资结算页存在学部、年级、搜索老师、选择老师四类筛选控件 |
| 财务端薪资后端数据 | 通过 | `T0001 / 李明` 薪资接口返回已锁定，实发 `8554.08`，5 节有效课时 |
| 财务端薪资页面渲染 | 通过 | 页面显示应发 `¥8,664`、实发 `¥8,554`、状态“已锁定”，不再停留在读取中 |
| 工资导出接口 | 通过 | `/api/payroll/export?month=2026-06&stageId=primary&grade=1&search=FY0001` 返回 `total=1` |
| 手机端课表 | 当前版通过 | 用户确认课表按当前版本作为最新版，暂不继续改单日时间线 |

本轮补测生成的本地样例数据：

| 字段 | 值 |
| --- | --- |
| 老师 | `T0001 / 李明 / teacher0001` |
| 排课范围 | 小学部一年级，10 个班 |
| 发布课节 | `200` |
| 老师当周课节 | `5` |
| 完成课节 | `5` |
| 工作量确认状态 | `locked` |
| 工资状态 | `locked` |
| 应发工资 | `8664` |
| 实发工资 | `8554.08` |

## 6. 已发现问题

### BUG-20260618-001 生产域名 DNS 解析错误

| 字段 | 内容 |
| --- | --- |
| 严重级别 | P0 阻塞 |
| 模块 | DNS / 生产环境 |
| 当前状态 | 暂缓处理：用户确认二维码和生产域名相关先不管 |
| 现象 | `fuyuanschool.org` 解析到 `198.18.0.20`，`www.fuyuanschool.org` 解析到 `198.18.0.19`；浏览器打开生产域名返回 `ERR_CONNECTION_CLOSED` |
| 影响 | 普通用户无法通过 `https://fuyuanschool.org` 访问系统；手机扫码和生产页面测试无法按真实域名继续 |
| 期望 | `fuyuanschool.org` 和 `www.fuyuanschool.org` 都解析到服务器公网 IP `47.76.189.5` |
| 已验证 | 强制解析到 `47.76.189.5` 后，HTTPS 证书、首页、接口、教室页均可正常返回 |
| 下一步 | 到 Namecheap DNS 配置中修改 A 记录：`@ -> 47.76.189.5`，`www -> 47.76.189.5` 或 `www CNAME @`，等待 DNS 生效后复测 |

### BUG-20260618-002 生产 readiness 失败：未发布课表且未锁定工资

| 字段 | 内容 |
| --- | --- |
| 严重级别 | P0 |
| 模块 | 数据初始化 / 行政排课 / 薪资锁定 / readiness |
| 现象 | `/api/phase1/readiness` 返回 `passed=false`，失败项为 `schedule_conflict`、`teacher_schedule`、`payroll_lock` |
| 复现 | 使用生产强制解析登录财务后调用 `/api/phase1/readiness` |
| 实际结果 | 当前发布课次为 0；已生成工资 0 份，已锁定工资 0 份 |
| 预期结果 | 试运行环境应至少有一套已发布无冲突课表，并至少有已生成/已锁定的样例工资数据，readiness 应通过 |
| 初步判断 | 自动化脚本在内存中会生成、发布、锁定，但生产运行库没有初始化主流程样例数据 |
| 建议 | 增加“生成试运行验收数据”脚本，或由行政完成一次配置、生成、发布，再由老师确认、教务/总校审批、财务生成并锁定一名老师工资 |

### BUG-20260618-003 手机端课表仍偏周视图，不是单日时间线

| 字段 | 内容 |
| --- | --- |
| 严重级别 | P1 |
| 模块 | 老师端 / 我的课表 / 手机端 |
| 当前状态 | 已按当前版本验收：用户确认课表就按照现在的，是最新版 |
| 现象 | 390px 宽度下，页面没有横向溢出，但仍显示整周日期和多天“无排班”结构 |
| 复现 | iPhone 宽度模拟，老师账号进入“我的课表” |
| 实际结果 | 页面出现周一至周日、多个时间段、多个“无排班” |
| 预期结果 | 点击某一天后，只显示该天时间线；一个屏幕能清晰展示一天课程 |
| 建议 | 移动端默认改成“日期横向选择 + 单日时间线”，仅桌面端保留周视图 |

### BUG-20260618-004 教室二维码页直接展示二维码 JSON 原文

| 字段 | 内容 |
| --- | --- |
| 严重级别 | P2 |
| 模块 | 教室大屏 / 二维码展示 |
| 当前状态 | 暂缓处理：二维码相关先不管 |
| 现象 | 教室屏除了二维码，还直接显示完整 JSON payload、nonce、signature |
| 影响 | 测试阶段方便排错，但正式教室电脑上观感不够干净，且不利于老师聚焦扫码 |
| 预期结果 | 正式模式只展示二维码、教室名、倒计时；JSON 原文收进“调试信息”折叠区 |
| 建议 | 增加调试模式开关，默认隐藏 payload 文本 |

### BUG-20260618-005 摄像头扫码未完成真机验证

| 字段 | 内容 |
| --- | --- |
| 严重级别 | P1 待验证 |
| 模块 | 老师端 / 签入签出 |
| 当前状态 | 暂缓处理：二维码和生产域名相关先不管 |
| 现象 | 桌面浏览器检查到签入、签出和授权输入兜底，但未完成手机摄像头权限和真实扫码 |
| 影响 | 不能确认 iPhone Safari、微信内置浏览器、Android Chrome 的摄像头授权和扫码识别稳定性 |
| 预期结果 | HTTPS 生产站点下手机可授权摄像头，能扫描教室动态二维码完成签入和签出 |
| 下一步 | 生产部署完成后，使用至少 3 台真机执行 TC-P0-016、TC-P0-017 |

### BUG-20260618-006 财务端薪资页面未渲染后端已锁定工资

| 字段 | 内容 |
| --- | --- |
| 严重级别 | P0 |
| 模块 | 财务端 / 薪资结算 |
| 当前状态 | 已修复 |
| 现象 | 财务账号进入“薪资结算”，选中 `李明 · 小学部 · 一年级 · 语文` 后，页面持续显示“读取中 / 正在生成薪资明细” |
| 已验证 | 后端接口 `/api/teachers/T0001/payroll?month=2026-06` 返回已锁定工资，`netPay=8554.08`，5 节有效课时数据完整 |
| 修复结果 | 财务端改为先读取已有工资，不再进入页面自动生成；页面已显示应发 `¥8,664`、实发 `¥8,554`、状态“已锁定” |
| 影响 | 财务人员无法在页面确认老师工资结果，即使后端数据已经存在 |
| 预期 | 页面应显示应发工资、代扣个税、实发工资、锁定状态和导出入口；已锁定工资不应继续显示“正在生成” |
| 初步判断 | 前端薪资明细加载状态或错误分支未正确结束；也可能是已锁定工资场景下仍触发 generate 流程 |
| 下一步 | 后续修复时先定位 `loadBackendTeacherDetail()`、`loadBackendPayroll()` 与薪资结算渲染状态 |

### BUG-20260618-007 老师薪资接口返回内容超过“只展示总薪资”的产品口径

| 字段 | 内容 |
| --- | --- |
| 严重级别 | P1 |
| 模块 | 老师端 / 薪资隐私 / API |
| 当前状态 | 已修复 |
| 现象 | 老师端页面只显示总薪资，但老师角色请求 `/api/teachers/T0001/payroll?month=2026-06` 时，接口仍返回 `generated.summarySnapshot`、`generated.rowsSnapshot`、`confirmation.summarySnapshot` 等工资组成快照 |
| 已验证 | 老师无法访问其他老师工资，`T0002` 返回 `403`，但本人接口返回内容仍过多 |
| 修复结果 | 老师登录态、本人信息、课表、工作量、薪资接口中的教师对象已去除 `salaryProfile`；老师薪资接口只返回总实发、生成状态、确认状态 |
| 影响 | 与“教师端不要有每节课薪资明细，只有总薪资”的产品口径不完全一致；接口层仍可能泄露工资构成 |
| 预期 | 老师角色接口只返回本人、月份、总实发、生成状态、确认状态等摘要字段；财务角色才返回完整工资明细 |
| 下一步 | 后续修复时收窄 `teacherPayrollSummaryOnly()` 的返回字段，并补自动化断言 |

### BUG-20260618-008 排课 OR-Tools 未稳定命中可行解，依赖 fallback 成功

| 字段 | 内容 |
| --- | --- |
| 严重级别 | P2 |
| 模块 | 行政排课 / 求解器 |
| 现象 | 本地生成小学部一年级课表时，OR-Tools CP-SAT 返回 `CP_SAT_NO_SOLUTION` 或未在限制时间内找到可行解，系统 fallback 到 `advanced-constraint-search` 后成功生成 200 节无冲突课表 |
| 影响 | 当前最终结果可用，但工业级求解器没有稳定承担主求解职责；复杂约束增多后可能影响性能和可解释性 |
| 预期 | 常规规模下 CP-SAT 应能稳定给出可行解，fallback 只作为兜底 |
| 下一步 | 后续优化 CP-SAT 变量建模、时间限制、软约束权重和失败原因解释，并把 fallback 原因展示给行政 |

## 7. 下一步计划

1. 生产域名和二维码相关暂缓，恢复处理时先修正 Namecheap DNS：`@` 和 `www` 指向 `47.76.189.5`。
2. DNS 生效后执行：

```bash
curl -sS https://fuyuanschool.org/api/health
```

3. 在生产站点重新执行：
   - 财务登录。
   - 行政登录。
   - 老师登录。
   - `/api/phase1/readiness`。
   - 教室二维码页。
   - 手机 HTTPS 摄像头扫码。
4. 处理 `BUG-20260618-002`：给试运行环境初始化一套发布课表和一条已锁定工资，或让行政/老师/财务完成一次真实主流程。
5. `BUG-20260618-003` 已按用户确认暂不处理，课表按当前版本作为最新版验收。
6. `BUG-20260618-004`、`BUG-20260618-005` 与二维码/真机扫码相关，暂缓。
7. `BUG-20260618-006` 已修复：财务薪资结算页可渲染已锁定工资。
8. `BUG-20260618-007` 已修复：老师薪资接口只返回总薪资摘要。
9. 处理 `BUG-20260618-008`：后续优化 CP-SAT 命中率和 fallback 可解释性。
