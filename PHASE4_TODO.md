# 第四阶段开发 TODO（交互体验终版打磨）

更新时间：2026-07-09
状态：**未启动**——第三阶段（3E，2027-01 底）收口且稳定运行 ≥ 2 周后进场
PRD：`docs/交互体验终版打磨-第四阶段PRD.md`

> 定性：纯体验阶段，零新增业务能力，"有余力再做"。全程可被业务需求打断，每批次独立上线独立回退。
> 已由 2026-07 P0/P1 交付、**不要重复做**：通用弹层（openDialog 系）、骨架屏（HR/财务 3 列表）、错误重试、⌘K 命令面板（功能页+人员）、SSE 推送基座（审批/通知）。

## 入场条件（缺一不开工）

- [ ] 3E 验收通过，全量生产稳定运行 ≥ 2 周，无 P0/P1 业务缺陷在修。
- [ ] 体验反馈收集完成：各角色 ≥ 3 人访谈 + 高频路径统计，产出《体验反馈清单》。
- [ ] 依据反馈清单重排下方批次优先级并冻结范围。

## 4A 反馈深度：乐观更新与撤销（默认 P0，约 1 周）

- [ ] 乐观更新白名单评审（金额/审批状态跃迁操作一律排除）。
- [ ] FB-1 白名单操作乐观更新 + 失败自动回滚提示（通知已读、待办勾选、筛选切换、工资确认）。
- [ ] FB-2 可撤销 toast（5 秒窗口，超时才提交）：撤回审批、删除草稿学期等非终态动作。
- [ ] FB-3 骨架屏推广到全部异步列表（通知/审计/工资记录/课表）。
- [ ] FB-4 全站写操作按钮 loading 态收口。

## 4D 移动端原生感与 PWA（默认 P0，约 1.5 周；依赖 3A 全员考勤已上线）

- [ ] MB-1 列表页下拉刷新。
- [ ] MB-2 移动端行内操作改底部 action sheet（审批、状态变更）。
- [ ] MB-3 PWA：manifest + SW 壳缓存 + 添加到主屏 + 明确离线态（不做离线数据）。
- [ ] MB-4 按压态/涟漪、safe-area 复查、键盘遮挡修正。
- [ ] iOS Safari + Android Chrome 真机验证记录。

## 4B 动效与弹层扩展（默认 P1，约 1.5 周）

- [ ] MO-1 视图切换/列表进出/面板展开 CSS 过渡；`prefers-reduced-motion` 全局豁免。
- [ ] MO-2 三热点列表（通知/审批/财务教师）增量 DOM 更新，消除整页闪烁（**不引框架**）。
- [ ] MO-3 桌面端右侧抽屉：档案详情/审批详情/工资单详情。
- [ ] MO-4 人员 hover 信息卡 + 图标按钮 tooltip 统一。

## 4C 搜索与键盘效率（默认 P1，约 1 周）

- [ ] SE-1 命令面板扩展：审批单/通知/动作命令（带权限过滤）。
- [ ] SE-2 面板空态显示最近访问。
- [ ] SE-3 快捷键体系：`?` 速查、`g+字母` 跳转、列表 j/k/Enter；输入框聚焦自动失效。
- [ ] SE-4 表头点击排序 + 吸顶全覆盖；千行级列表虚拟滚动评估（仅评估，结论进文档）。

## 4F 空态、引导与可访问性（默认 P1，约 1 周，放最后收口）

- [ ] PO-1 全站空态升级：SVG 插画 + 说明 + 行动按钮。
- [ ] PO-2 各角色首登新手引导（3~4 步，可跳过可重看）+ 简版帮助页。
- [ ] PO-3 可访问性基线：焦点可见、弹层 focus trap、关键流程 aria、对比度 WCAG AA。
- [ ] PO-4 全站一致性终审：文案/日期金额格式/按钮位置/危险色，走查并修偏差。

## 4E 实时协同扩展（默认 P2，约 0.5~1 周）

- [ ] LV-1 SSE 主题扩展：排课任务完成、工资发布、OA 审批结果。
- [ ] LV-2 编辑互斥提示（提示不锁死，后端版本校验兜底）。
- [ ] LV-3 SSE 接 Redis pub/sub（仅在决定多实例部署时执行，否则降级为文档确认）。

## 明确不做（红线，见 PRD 第 3 节）

前端框架重写、拖拽调课（属业务需求另立项）、深色模式、原生 App、离线数据同步。

## 每批次收口口径（全部批次通用）

- [ ] `npm run check` + 全部既有测试套件通过（上线前后各一次）。
- [ ] 三视口（375/1000/1280）浏览器走查。
- [ ] 性能预算达标：首屏可交互 ≤ 2s、CLS≈0、app.js 体积累计增幅 ≤ 15%。
- [ ] 缓存版本号升级 + CHANGELOG 记录。
- [ ] 3~5 名真实用户抽验回访，结论记入验收单。
